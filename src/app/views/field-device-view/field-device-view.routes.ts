import { Route } from '@angular/router';
import { FieldDeviceViewComponent } from './field-device-view.component';

export const routes: Route[] = [
    {
        path: '',
        component: FieldDeviceViewComponent,
    },
];
